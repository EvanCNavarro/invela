import React from 'react';

/**
 * Empty placeholder component that replaces download buttons
 * while the file generation functionality is being fixed
 */
export const TaskPageDownloadPlaceholder: React.FC = () => {
  return null; // Return null to render nothing
};