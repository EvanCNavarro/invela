import { useAuth } from "@/hooks/use-auth";

export function NavigationMenu() {
  return (
    <div className="px-4 py-2 flex items-center border-b">
      <img src="/invela-logo.svg" alt="Invela" className="h-6" />
    </div>
  );
}