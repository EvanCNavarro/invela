import React from 'react';

/**
 * EmptyWelcomeModal component
 * 
 * This is a placeholder component that replaces the old WelcomeModal.
 * It renders nothing and is used to prevent import errors.
 */
export function WelcomeModal() {
  return null;
}